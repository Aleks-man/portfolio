# Add the production origin after purchasing the domain, without a trailing slash.
# This value is used for canonical, Open Graph, hreflang, robots.txt, and sitemap.xml.
VITE_SITE_URL=https://example.ru
